## Virtual Network Subnet Delegation Settings 
## ==============================
## Pre-configured example settings for virtual network subnet delegation resources

locals {
  virtual_network_subnet_delegation_settings = {
    abbreviation = "del"
  }
}
